import React from "react";
import ReactDOM from "react-dom/client";
import "./index.css";
import Contact from "./contact-index";

const rootContact = ReactDOM.createRoot(
  document.getElementById("root-contact")
);
root.render(<Contact />);
